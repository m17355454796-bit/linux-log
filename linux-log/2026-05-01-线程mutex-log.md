今天学习了锁的基本实现思想。
互斥锁的底层可以抽象理解为一个状态变量。1 表示锁空闲，0 表示锁被占用。线程加锁时，会通过 CPU 提供的原子交换指令尝试抢锁。如果交换得到的旧值是 1，说明锁原本空闲，加锁成功；如果得到的是 0，说明锁已经被其他线程占用，加锁失败，需要等待。解锁时，将锁状态重新设置为 1。
需要注意的是，真实的 pthread_mutex_t 内部实现比简单的 0/1 整型变量复杂，可能涉及 futex、等待队列、线程阻塞与唤醒等机制。0/1 变量只是帮助理解互斥锁工作原理的抽象模型。