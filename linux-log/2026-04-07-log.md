今天我配置好了vim，可以更好的写cpp程序了
学习了gcc c语言文件的四个流程
预处理：把宏替换掉，头文件展开，处理条件扩展，预处理完的文本质上还是c语言文件
其中条件扩展是指可以通过条件执行部分函数，在某些软件中可以用一份文件区分免费版用户和会员，让他们享受不一样的服务
编译：这一步就是把预处理完的c语言文件变成汇编语言
汇编：这一步就是把汇编语言变成二进制文件
链接：最后一步，把二进制文件变成可执行程序
其中前三步在gcc后面分别加上-E,-S,-c选项即可，在加上—o选项，分别变为.i，.s，.o文件
