今天我学习了vim先关的内容，知道了vim里面的三个端口
一个是插入模式，可以像word一样编写文字，另一个是命令模式，可以进行一些快速的操作，还有一种是底行模式，相当于命令模式，也可以进行一些操作
一般vim进入文件之后，我们进入的都是命令模式，我们可以用i直接进入插入模式，也可以用o和a进入插入模式，差别只有进入插入模式后光标的位置不同，没有多大区别，二从命令模式进入底行模式的话，我们直接输英文版的:就行了，再在底行输入相关的操作命令，而从底行模式或者插入模式进入命令模式的话，我们直接按esc按键即可，而底行模式和插入模式是不互通的，只能通过命令模式来进行切换。由此可见，命令模式是最重要的模式。
其中命令模式中，我学习了gg，直接进入开头，G，直接进入末尾m（一个数字） G，直接进入第m行。
yy 进行复制 m yy 对该行复制m次
pp 直接粘贴
w 光标向右移动一个单词
b 光标向左移动一个单词
dd 对改行进行剪切
dw 对该单词进行剪切
m dd 剪切改行后面的m行
$ 直接到该行的最后一个位置
0 到该行的第一个第一个位置
^ 到该行的第一个单词的首字母位置
u 对操作进行撤销
ctrl+r 对上一个撤销进行撤销
x 删除一个字符
m x删除x个字符
dw 删除一个单词
h 向左移动光标
j 向下移动光标
k 向上移动光标
l 向右移动光标
这些字母加上数字就代表向某位置移动了多少光标
小写的p是往该行下边粘贴
大写的P是往该行上面粘贴

